
  // const pai = element.parentNode;

  // const allClasses = pai.className.split(/\s+/); // divide por espaços
  // const classesWithDot = allClasses.filter((cls) => cls.startsWith("."));
  // const classFather = classesWithDot.join('');
  // const funther = document.querySelector('.card');
  // console.log(funther);
  // if (element.hasChildNodes()) {
  //   Array.from(element.childNodes).forEach((child) => {
  //     const node = child.cloneNode(true);
  //     buildComponent.append(node);
  //   });
  // }

